# ■ 7-Day Intensive Data Pipeline & SQL 실전 마스터 플랜
**목표:** 공공데이터 확보부터 파이썬 전처리, SQL 분석, 태블로(Tableau) 시각화 대시보드 구축까지 '엔드 투 엔드(End-to-End)' 실무 사이클 1주일 내 완성

---

## ■ Day 1: 데이터 확보 및 자동화 파이프라인 구축 (Python)
**■ 목표:** 무거운 원본 데이터를 다루기 쉬운 형태로 쪼개고 정제하기

* **[Task 1] 실습 데이터 다운로드**
  * **소스:** 소상공인시장진흥공단 상가(상권)정보 (공공데이터포털)
  * **선정 이유:** 데이터 용량이 크고, 지역별/업종별로 테이블을 분리하여 `JOIN` 실습하기에 가장 최적화되어 있음.
* **[Task 2] 개발 환경 세팅**
  * Python 및 VS Code 설치
  * 터미널에서 필요 라이브러리 설치: `pip install pandas openpyxl`
* **[Task 3] Python 스크립트 작성 및 실행**
  * 다운로드한 대용량 엑셀/CSV 데이터를 Pandas로 불러오기.
  * 불필요한 열(Column) 제거 및 결측치 1차 처리.
  * 서울, 경기, 부산 등 '지역별' 혹은 '업종별'로 데이터를 분할하여 각각의 `.csv` 파일로 자동 저장하는 스크립트 실행.

---

## ■ Day 2: 로컬 데이터베이스 구축 및 적재 (DB Setup)
**■ 목표:** 사내 보안을 우회하는 독립된 로컬 DB를 띄우고 데이터 밀어 넣기

* **[Task 1] 무설치 DB 실행**
  * MySQL ZIP Archive 버전 다운로드 및 바탕화면에 압축 해제.
  * 명령 프롬프트(cmd)에서 `mysqld --initialize-insecure --console` 실행하여 비밀번호 없는 서버 세팅.
* **[Task 2] 스키마 및 테이블 설계 (ERD)**
  * MySQL Workbench 접속 (비밀번호 공란).
  * `CREATE SCHEMA local_commerce;` 실행.
  * '상권정보', '업종코드', '행정구역' 테이블 간의 연결 고리(Primary Key, Foreign Key) 구상.
* **[Task 3] 데이터 Import**
  * Table Data Import Wizard를 사용하여 Day 1에서 만든 CSV 파일들을 각각의 테이블로 업로드.
  * `SELECT * FROM 테이블명 LIMIT 10;`으로 한글 깨짐 없이 적재되었는지 확인.

---

## ■ Day 3: SQL 기본기 및 데이터 결합 (SELECT & JOIN)
**■ 목표:** 흩어진 데이터를 하나의 의미 있는 뷰(View)로 합치기

* **[Task 1] 기초 데이터 탐색 (CRUD 중 R)**
  * `SELECT`, `FROM`, `WHERE`를 사용하여 특정 구(예: 강남구)의 데이터만 필터링.
  * `LIKE` 연산자를 써서 특정 키워드(예: '%카페%')가 포함된 상호명 찾기.
* **[Task 2] JOIN 마스터하기 (핵심 ■)**
  * `INNER JOIN`: 상권 테이블과 업종 테이블을 '업종코드'를 기준으로 결합하여 완벽하게 매칭되는 데이터만 추출.
  * `LEFT JOIN`: 행정구역 데이터에 상권 데이터를 붙여서, 상권이 아예 없는(NULL) 구역도 누락 없이 확인해 보기.

---

## ■ Day 4: 데이터 요약 및 범주화 (GROUP BY & CASE WHEN)
**■ 목표:** Raw Data를 비즈니스 인사이트로 압축하기

* **[Task 1] 데이터 그룹화 및 집계**
  * `GROUP BY`와 집계 함수(`COUNT`, `SUM`, `AVG`) 사용.
  * "각 자치구별로 프랜차이즈 매장이 가장 많은 곳은 어디인가?" 쿼리 작성.
  * `HAVING`을 사용하여 매장 수가 500개 이상인 자치구만 필터링.
* **[Task 2] 조건부 로직 적용**
  * `CASE WHEN`을 사용하여 데이터 범주화.
  * 예: "매장 면적이 50㎡ 미만이면 '소형', 50~100㎡면 '중형', 그 이상이면 '대형'으로 구분하는 새로운 파생 열(Column) 만들기."

---

## ■ Day 5: 고급 SQL 및 데이터 추출 (Window Function)
**■ 목표:** RA 면접 프리패스용 고급 쿼리 스킬 장착

* **[Task 1] 윈도우 함수 활용 (핵심 ■)**
  * `ROW_NUMBER()` 또는 `RANK()` 함수 사용.
  * "각 구별로 커피 전문점이 가장 많이 밀집된 행정동 Top 3를 순위 매겨서 추출하기." (단순 GROUP BY로는 해결할 수 없는 고급 문제)
* **[Task 2] 서브쿼리(Subquery)와 CTE(WITH 절)**
  * 쿼리 안에 쿼리를 넣는 방식 연습.
  * 코드를 깔끔하게 읽히게 만드는 `WITH` 구문을 활용하여 복잡한 로직을 단계별로 분리해서 작성.

---

## ■ Day 6: 실전 비즈니스 문제 해결 및 데이터 Export
**■ 목표:** 가상의 클라이언트 질문에 답하고 최종 데이터셋 뽑아내기

* **[Task 1] 비즈니스 쿼리 3제 (스스로 묻고 답하기)**
  1. 최근 1년 내 폐업률이 가장 높은 요식업 카테고리는?
  2. 강남구 내 2030 유동 인구(공공데이터 추가 결합 시)와 헬스장 분포의 상관관계는?
  3. 프랜차이즈와 개인 매장의 생존 기간 차이는?
* **[Task 2] 최종 데이터 추출**
  * 대시보드 시각화에 딱 맞게 정제된 최종 결과물 쿼리를 실행.
  * 해당 결과를 다시 `.csv` 파일로 Export (저장).

---

## ■ Day 7: 의사결정용 시각화 대시보드 구축 (Tableau)
**■ 목표:** 추출한 데이터를 경영진이 조작해 볼 수 있는 화면으로 구성

* **[Task 1] Tableau Public 연동**
  * Day 6에서 Export한 최종 CSV 파일을 Tableau Public에 연결.
* **[Task 2] 핵심 차트 3개 제작**
  * **Bar Chart:** 구별 업종 밀집도 비교 (순위 정렬).
  * **Map(지도) 시각화:** 위도/경도 데이터를 활용해 지도 위에 매장 분포를 점(마크)으로 뿌려보기.
  * **Pie/Donut Chart:** 대형 vs 중소형 매장 비율.
* **[Task 3] 인터랙티브 대시보드 조립**
  * 3개의 차트를 하나의 대시보드 캔버스에 배치.
  * '자치구' 필터를 적용하여, 특정 구를 클릭하면 나머지 차트들이 해당 구의 데이터로 즉각 연동되어 변하도록(Action) 세팅.
